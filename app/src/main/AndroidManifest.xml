<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.gbax.lighttraining" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.ProgramManagerActivity"
            android:label="@string/title_activity_create_program" >
        </activity>
        <activity
            android:name=".activity.CreatingProgramActivity"
            android:label="@string/title_activity_creating_program" >
        </activity>
        <activity
            android:name=".activity.CreatingExerciseActivity"
            android:label="@string/title_activity_creating_exercise" >
        </activity>
        <activity
            android:name=".activity.ExerciseListActivity"
            android:label="@string/title_activity_exercise_list" >
        </activity>
    </application>

</manifest>
